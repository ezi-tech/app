import { SafeAreaView, Text } from "react-native";

export default function SettingsScreen() {
  return (
    <SafeAreaView>
      <Text className="text-red-500 text-3xl">This is the settings screen</Text>
    </SafeAreaView>
  )
}