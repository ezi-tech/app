import { SafeAreaView, Text } from "react-native";

export default function SupportScreen() {
  return (
    <SafeAreaView>
      <Text className="text-red-500 text-3xl">This is the support screen</Text>
    </SafeAreaView>
  )
}