import { SafeAreaView } from "react-native";
import { Text } from "react-native";

export default function OrderScreen() {
  return (
    <SafeAreaView>
      <Text className="text-red-500 text-3xl">This is the order screen</Text>
    </SafeAreaView>
  );
}
