import { SafeAreaView, Text } from "react-native";

export default function AddressesScreen() {
  return (
    <SafeAreaView>
      <Text className="text-red-500 text-3xl">This is the addresses screen</Text>
    </SafeAreaView>
  )
}